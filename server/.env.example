# Example environment configuration for VoiceUp Backend
# Copy this file to .env and fill in real values (never commit the real .env)

# MongoDB connection string (local dev database)
MONGO_URI=mongodb://localhost:27017/voiceup_dev

# JWT secrets (use long, random strings in production)
JWT_ACCESS_SECRET=change_me_access_secret_32+chars
JWT_REFRESH_SECRET=change_me_refresh_secret_32+chars

# Google OAuth credentials (create at console.cloud.google.com)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ImageKit (media upload) credentials
IMAGEKIT_PUBLIC_KEY=your_imagekit_public_key
IMAGEKIT_PRIVATE_KEY=your_imagekit_private_key
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_path

# Gemini (AI scoring) API key
GEMINI_API_KEY=your_gemini_api_key

# Runtime
NODE_ENV=development
PORT=3000

# Optional cookie security override (default true in code). Set to false ONLY for local http testing.
# COOKIE_SECURE=false

# General tips:
# - NEVER commit real secrets
# - Use different secrets per environment (dev/stage/prod)
# - Regenerate tokens if you suspect compromise

# Add any additional config keys below as the project grows
